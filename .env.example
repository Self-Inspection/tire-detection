# Server-side OpenAI key (required — not exposed to the browser)
OPENAI_API_KEY=sk-your-key-here

# Optional: override vision model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o

# Local dev: run API server in a second terminal
# npm run dev:server
# npm run dev
