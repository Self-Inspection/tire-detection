# Server-side OpenAI key (recommended for Railway — not exposed to the browser)
OPENAI_API_KEY=sk-your-key-here

# Optional: override default vision model (gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Local dev: run API server in a second terminal
# npm run dev:server
# npm run dev
