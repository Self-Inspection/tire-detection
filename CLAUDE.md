# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is an early-stage application for detecting tire grooves. The repository currently contains only a README — no build system, dependencies, or application code have been added yet.

## Current State

The project is a blank slate. When implementation begins, update this file with:
- The chosen language/framework and how to install dependencies
- Build, lint, and test commands (including how to run a single test)
- Architecture decisions (model selection, inference pipeline, API design, etc.)
- Any data pipeline or training workflow specifics
